# IR0 0.0.1 — ABI contract board (userspace moderno)

> **Last verified:** 2026-09-05  
> **Source of truth:** `scripts/linux_abi/contracts.json`, `make linux-abi-audit`,  
> `Documentation/ai_driven_dev/linux_ground_truth.md`, `make smoke-release-0.0.1`

Estados por iteración (regla local `~/.cursor/rules/kernel-linux/ir0-contract-iteration-states.mdc`):

| Estado | Significado |
|--------|-------------|
| **VERIFIED** | Audit Linux automatizado PASS + contrato enabled |
| **LINUX-LIKE** | ktest/smoke OK; audit pendiente o parcial |
| **BLOCKED** | Primera divergencia Linux↔IR0 documentada |
| **EXPERIMENTAL** | Fuera del certificado 0.0.1 (TCC, Doom, etc.) |

## Resumen

| Estado | Contratos | % cola 0.0.1 |
|--------|-----------|--------------|
| **VERIFIED** | 17 | **16/15 cola** + wait4_wnohang + vfs_write + sigreturn_blocked_syscall |
| **LINUX-LIKE** | 0 | — |
| **BLOCKED** | 0 | — |
| **EXPERIMENTAL** | 2 | no cuenta para rc/final |

**Cola prioritaria 0.0.1 (15):** brk, wait4, read, mmap, mount, execve, open/close, stat/fstat, dup/dup2, pipe, poll, nanosleep, getcwd, unlink/rename, chdir — **audit completa**.

**Bundle R3 `vfs_write`** cubre unlink/rename/lseek/truncate individuales.

---

## VERIFIED (audit PASS)

| Contrato | Gate | Notas |
|----------|------|-------|
| brk | `linux-abi-audit` / `diag-contract CONTRACT=brk` | delta 4 KiB; VA absoluta puede diferir |
| wait4 | `linux-abi-audit-wait4` | fork + `_exit(42)` + status word |
| **wait4_wnohang** | `linux-abi-audit-wait4-wnohang` | WNOHANG alive → exitack → block reap → ECHILD |
| read | `linux-abi-audit-read` | pipe/EOF/EBADF |
| **pipe** | `linux-abi-audit-pipe` | reusa `read_probe` + ktest `syscall_pipe` |
| **poll** | `linux-abi-audit-poll` | POLLIN on pipe |
| **nanosleep** | `linux-abi-audit-nanosleep` | 2 ms → ret 0 |
| **getcwd** | `linux-abi-audit-getcwd` | cwd `/` |
| **chdir** | `linux-abi-audit-chdir` | `chdir("/")` + getcwd |
| **sigreturn_blocked_syscall** | `linux-abi-audit-sigreturn-blocked-syscall` | pipe read + SIGUSR1: EINTR (no SA_RESTART) + SA_RESTART |
| **dup / dup2** | `linux-abi-audit-dup` | clear FD_CLOEXEC on duplicate (man 2 dup); EBADF; path `/proc/uptime` (fd_table real) |
| mmap | `linux-abi-audit-mmap` | anon RW, PROT_NONE, MAP_FIXED, munmap, bad fd |
| mount | `linux-abi-audit-mount` | tmpfs `/tmp/ir0mnt` |
| openat / close | `linux-abi-audit-openat` | `/proc/uptime`, ENOENT, EBADF |
| stat / fstat | `linux-abi-audit-stat` | `/proc/uptime`, `/sbin/init`, ENOENT |
| execve | `linux-abi-audit-execve` | helper + ENOENT |
| **vfs_write (R3)** | `linux-abi-audit-vfs-write` | 29/29 PASS; tmpfs |

---

## LINUX-LIKE

| Contrato | Evidencia | Próximo paso |
|----------|-----------|--------------|
| — | — | — |

---

## BLOCKED

| Contrato | Divergencia | Fix mínimo |
|----------|-------------|------------|
| — | — | — |

---

## EXPERIMENTAL (no gate 0.0.1)

| Área | Motivo |
|------|--------|
| TCC in-guest | `smoke-fase52-tcc` cuelgue en link estático |
| Doom / tier-2 gfx | fuera del release gate D1.20 |

---

## Release gate vs audit

| Gate | Incluye audit |
|------|----------------|
| `make smoke-release-0.0.1` | `linux-abi-audit` (todos los enabled) |
| `make linux-abi-audit` | **16** contratos enabled |
| `make agent-fast` | host + ktest (~20s) |
| `make diag-contract CONTRACT=…` | ISO fresco + un contrato |

**FAT16 rw:** no habilitar hasta `linux-abi-audit-vfs-write` verde **sobre backend FAT16**.

Fuente ABI dup CLOEXEC: [man 2 dup](https://man7.org/linux/man-pages/man2/dup.2.html).

**ARCH-3 (2026-07-10):** opens de `/proc` y `/sys` (registry + dynamic pid status/cmdline + dirs `/proc/pid`) instalan slots reales en `fd_table` (`is_pseudo` / dir bind). Ya no se asignan fds globales `1001`/`1010`/`1150`/`1151` ni `PSEUDO_FS_*_FD_BASE` a userspace. `PSEUDO_FS_*_FD_BASE` queda como ID interno de tabla registry.

**ARCH-3 residual (2026-07-10):** eliminadas ramas I/O de `FD_SYS_BASE`/`FD_DEV_BASE` (read/write/fstat/getdents/poll/close/lseek/ioctl/ftruncate/mmap fb0). Solo `fd_table` con `is_pseudo` / `is_devfs`. Macros `FD_*_BASE` retirados de `syscalls_glue.h`.
