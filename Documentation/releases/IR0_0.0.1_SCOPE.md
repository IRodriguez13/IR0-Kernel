# IR0 0.0.1 — Release scope (honest inventory)

> **Last verified:** 2026-07-30  
> **Source of truth:** `make smoke-release-0.0.1`, `Documentation/releases/IR0_0.0.1_SCOPE.md`,  
> `scripts/linux_abi/contracts.json`, `kernel/test/`, `tests/host/`, Makefile smoke targets,  
> last pre-release tag [`IR0_0.0.1_RC4.md`](IR0_0.0.1_RC4.md) (`v0.0.1-rc4`)

**Post-rc4 policy:** until final `v0.0.1`, only bugfixing and stabilization of the existing
0.0.1 surface — no new product features for this ship.

## Release gate (D1.20)

```bash
make smoke-release-0.0.1    # primary 0.0.1 gate
make release-0.0.1            # + kernel-text-budget
```

**Included gates (deterministic):**

| Gate | Validates |
|------|-----------|
| `roadmap-phase1-stability` | `kernel-x64.bin`, host tests, `kernel-tests`, memsafe, analyze, matrix-min, arch-guard, `smoke-mm-cow-lazy` |
| `linux-abi-audit` | Linux↔IR0 `brk` ground truth + host/ktest evidence |
| `smoke-runit-ash-interactive` | runit + BusyBox ash + TTY canonical read (`echo hi`) |
| `smoke-fat16-mount` | FAT16 read-only on `/dev/hdb` |

**Explicitly removed from release gate (legacy / flaky / diagnostic):**

- `smoke-fase52-tcc` — **WARN/BLOCKER** if required manually; hangs at first static link in QEMU (see STABLE.md)
- `smoke-fase55*` Doom stubs — optional tier-2; not part of 0.0.1 core
- `IR0_LEGACY_SMOKE=1` fase53b-posix-pseudofs — superseded by ktests + tier smokes

---

## 1. Features included in 0.0.1

### MM / mmap / brk

| Item | Status | Gate | Limitations |
|------|--------|------|-------------|
| Lazy anon mmap (`CONFIG_LAZY_ANON_MMAP`) | **VERIFIED** | `smoke-mm-cow-lazy`, ktest `mmap_null_placement` | No MAP_SHARED file-backed COW at Linux parity |
| Lazy brk heap (`CONFIG_LAZY_BRK_HEAP`) | **VERIFIED** | same + ktest `brk_post_exec`, `linux-abi-audit` | Initial break layout differs from Linux absolute VA; delta semantics match |
| mmap NULL top-down + stack gap | **VERIFIED** | host + ktest `mmap_null_placement` | Gap size policy in `mmap_contract.h`; not full Linux ASLR |
| Fork COW (hybrid) | **VERIFIED** | `smoke-mm-cow-lazy` FASE40 A–F | No 2 MiB huge-page COW |
| PMM frame refcount | **VERIFIED** | ktests + FASE40 smoke | — |

### wait4 / process lifecycle

| Item | Status | Gate | Limitations |
|------|--------|------|-------------|
| Target-specific `wait4(pid, …)` | **VERIFIED** | ktest `wait4_specific_*` (D1.17) | No full POSIX wait options audit |
| Zombie reap + destroy | **PARTIAL** | ktests `wait4_*` | Orphan reparent logs still noisy in ktest boot |
| `exit` / status encoding | **PARTIAL** | ktest `wait4_status` | Not full shell job control |

### TTY canonical read

| Item | Status | Gate | Limitations |
|------|--------|------|-------------|
| Blocking canonical read + wake | **VERIFIED** | ktest `tty_canon_*`, `smoke-runit-ash-interactive` | Integration smoke can be timing-sensitive (see risks) |
| `/dev/console` on framebuffer | **VERIFIED** | ash interactive smoke | Not full PTY master/slave pair |

### VFS / tmpfs / mount

| Item | Status | Gate | Limitations |
|------|--------|------|-------------|
| tmpfs mount path resolution | **VERIFIED** | ktest `mount_tmpfs_contract` | `runtime-mount-check` in phase2, not release gate |
| MINIX root + inject rootfs | **VERIFIED** | all QEMU smokes | Single-root MINIX v1 |
| `rename` / `unlink` / `stat` | **PARTIAL** | host/ktest subsets | Not full Linux VFS parity |

### FAT16 read-only smoke

| Item | Status | Gate | Limitations |
|------|--------|------|-------------|
| Mount FAT16 on `/dev/hdb`, read file | **VERIFIED** | `smoke-fat16-mount` (D1.18) | Mount at `/fat16mnt` (root parent); **read-only** |
| Virtual `/dev/fat0` router | **PARTIAL** | ktest `mount_multi_fs_contract` | In-memory, not block BPB |

### Linux ABI audit infrastructure

| Item | Status | Gate | Limitations |
|------|--------|------|-------------|
| Orchestrator + contracts registry | **VERIFIED** | `make linux-abi-audit` | Infrastructure only |
| Automated contract: `brk` | **VERIFIED** | `linux-abi-audit` | Only one contract enabled; absolute brk VA may differ |
| Other syscalls in registry | **PARTIAL** | manual / future | Listed in `linux_ground_truth.md`, not automated |

### KTM flight recorder

| Item | Status | Gate | Limitations |
|------|--------|------|-------------|
| `[KTM][EV]` milestones | **VERIFIED** | boot smokes | Serial noise when `CONFIG_KTM_EVENTS=y` |
| Flight ring on panic | **VERIFIED** | `make ktm-check` (optional) | Panic-only dump |
| D1.14 malloc forensics | **EXPERIMENTAL** | off by default (`CONFIG_KTM_MALLOC_FORENSICS=n`) | Enable only for bisect |

### Host tests / ktests / smokes

| Item | Status | Gate | Limitations |
|------|--------|------|-------------|
| Host ABI suite (16 tests) | **VERIFIED** | `tests/host run` | musl cred, stat, mmap, signals, brk |
| In-kernel ktest suite (38 tests) | **VERIFIED** | `kernel-tests` | Requires `disk.img` |
| `smoke-mm-cow-lazy` | **VERIFIED** | release gate | FASE39 + FASE40 tags (contract, not D1 noise) |
| `smoke-runit-ash-interactive` | **PARTIAL** | release gate | See ash flake risk |

### BusyBox / ash runtime

| Item | Status | Gate | Limitations |
|------|--------|------|-------------|
| runit PID1 + BusyBox ash | **VERIFIED** | `smoke-runit-ash-interactive` | Not full applet matrix |
| Extended coreutils (fase58l) | **PARTIAL** | optional `smoke-fase58l-busybox-coreutils` | Not in release gate |
| musl static userspace ISO | **VERIFIED** | tier smokes | Tier-1 subset only |

### TinyCC / fase52

| Item | Status | Gate | Limitations |
|------|--------|------|-------------|
| TCC build in tree | **PARTIAL** | `build-tcc-fase52` manual | **`smoke-fase52-tcc` BLOCKER** — hangs in QEMU |
| In-guest `tcc -static` | **EXPERIMENTAL** | not in release gate | Do not claim “stable” until smoke green |

### COW / lazy heap

| Item | Status | Gate | Limitations |
|------|--------|------|-------------|
| Lazy + COW combined | **VERIFIED** | `smoke-mm-cow-lazy` | See MM section |

### Doom / T2 graphics

| Item | Status | Gate | Limitations |
|------|--------|------|-------------|
| `/dev/fb0`, `/dev/events0`, mmap fb | **PARTIAL** | legacy fase54–55 smokes | Not in D1.20 release gate |
| doomgeneric + WAD | **EXPERIMENTAL** | `REAL_WAD_PATH` optional | Tier-2 demo; not required for 0.0.1 |

### Signals

| Item | Status | Gate | Limitations |
|------|--------|------|-------------|
| `rt_sigaction` sigsetsize=8 | **VERIFIED** | host `signal_rt_sigaction_abi` | No full signal delivery matrix |
| SIGSEGV delivery ktest | **PARTIAL** | ktest `signal_segv_deliver_irq_frame` | — |

---

## 2. Explicitly out of 0.0.1 scope

| Feature | Reason |
|---------|--------|
| FAT16 write / create / truncate | Read-only smoke only |
| EXT2 full read/write | Not implemented |
| TCP complete stack | UDP/partial net only |
| PTY complete (`openpty`, job control) | Canonical TTY on console only |
| pthread / futex complete | `smoke-musl-pthread` not in release gate |
| Full POSIX / LTP parity | Tier-1 slice only |
| Desktop GUI / WM | T3 out of tree |
| Multi-user security (real shadow, PAM) | Partial cred ABI only |
| SMP | Single CPU |
| `linux-abi-audit` beyond `brk` | Infrastructure present; contracts pending |

---

## 3. Test & instrumentation classification (D1.20)

### Permanent regression tests (retained)

| Area | Host | Ktest | Smoke | ABI audit |
|------|:----:|:-----:|:-----:|:---------:|
| brk post-exec | `test_elf_initial_brk_abi` | `brk_post_exec` | FASE39 heap (lazy) | `linux-abi-audit` |
| mmap NULL / gap | `test_mmap_null_placement` | `mmap_null_placement` | FASE39 mmap | — |
| rt_sigaction sz=8 | `test_signal_rt_sigaction_abi` | — | — | — |
| wait4 target | — | `wait4_specific_*` | FASE40 fork (indirect) | — |
| TTY canon read | — | `tty_canon_*` | `smoke-runit-ash-interactive` | — |
| tmpfs mount | — | `mount_tmpfs_contract` | phase2 grep | — |
| FAT16 read | — | virtual fat0 | `smoke-fat16-mount` | — |

### Temporary diagnostics (gated off by default)

| Instrumentation | Config / gate | Default (release) |
|-----------------|-----------------|---------------------|
| `[D1.10]` stack-adjacent PF | `CONFIG_DEBUG_D1_DIAG` | **off** |
| `[D1.12]` read/syscall diag | `CONFIG_DEBUG_D1_DIAG` | **off** (header stubs; .o not linked) |
| `[D1.16]` TTY read diag | `CONFIG_DEBUG_D1_DIAG` | **off** |
| `[D1.14]` malloc/memmove PF | `CONFIG_KTM_MALLOC_FORENSICS` | **off** |
| `[PF_AUDIT]` / `[GPF_AUDIT]` | `CONFIG_DEBUG_PAGE_FAULTS` | **off** |
| `[PROBE]*` musl path | `CONFIG_KTM_PROBE_DIAG` | **off** |
| `[FASE40][COPY]` fork trace | `CONFIG_DEBUG_FORK` | **off** |
| `FASE40_D_AUDIT` wait/reap trail | `FASE40_D_AUDIT` macro | **off** (dead code remains) |
| `[MMAP_AUDIT]` / FASE39 serial | `CONFIG_DEBUG_MMAP_AUDIT` | **off** |

Release builds must not emit `[D1.*]` tags unless debug configs are explicitly enabled.

### Release gates (D1.20 bundle)

See top of this document.

### Deprecated / noisy (not in release gate)

| Item | Action |
|------|--------|
| `smoke-userspace-heap` / `smoke-userspace-mmap` | Legacy; use ktests + `smoke-mm-cow-lazy` |
| `smoke-fase52-tcc` | Documented BLOCKER; manual only |
| `smoke-fase55*` Doom | Optional tier-2 |
| `test_ktm_panic_inventory.c` | Orphan; use `ktm-check` script |
| CHANGELOG `fat16_bpb_probe` ktest | Stale doc; test never merged |

---

## 4. Known risks

| Risk | Severity | Mitigation |
|------|----------|------------|
| **ash interactive smoke flaky** | **BLOCKER** if fails on release run | Fail release; do not hide. Target 3/3 local runs before tag |
| **fase52-tcc hang** | **WARN** (out of gate) | Manual `build-tcc-fase52`; not claimed stable |
| **FAT16 read-only** | INFO | No write path in 0.0.1 |
| **linux-abi-audit: brk only** | INFO | Enable next contracts incrementally |
| **KTM_EVENTS serial noise** | LOW | Acceptable; not D1 forensics |
| **FASE40_D_AUDIT dead blocks in `process.c`** | LOW | Remove in follow-up ARCH-4 sprint |
| **FAT16 serial tag split** | LOW | Short `[FAT16OK]` tag + flat grep in Makefile |
| **Uncommitted D1.18/D1.19/D1.20 worktree** | PROCESS | Commit before release tag |

---

## 5. Commits / features (~last month)

| Date | Commit | Feature / fix |
|------|--------|---------------|
| 2026-06-26 | `2dbc886` | tmpfs mount path contract (MINIX root + resolve) |
| 2026-06-26 | `6591e73` | wait4 target-specific reaping (D1.17) |
| 2026-06-26 | `dee735d` | brk/mmap huge-page inheritance fix |
| 2026-06-23 | `7e57339` | TTY blocking canonical read via saved syscall frame (D1.16) |
| 2026-06-23 | `0dafac6` | rt_sigaction sigsetsize=8 UAPI (D1.15) |
| 2026-05-25 | fase58e–58l | BusyBox ash interactive, applets, coreutils smoke |
| 2026-05-24 | fase58c/d | devfs fb0 mmap, framebuffer console |
| 2026-05-24 | `2801cce` | smoke autokill harness |
| 2026-05-24 | `43e3bcb` | musl arch_prctl smoke |
| 2026-05-23–22 | FASE50–55 | userspace exec, musl ISO, Doom stack, sched split |
| *worktree* | D1.18 | FAT16 hdb read-only smoke (uncommitted) |
| *worktree* | D1.19 | Linux ABI audit infra + `brk` contract (uncommitted) |
| *worktree* | D1.20 | Release gate + debug gating + this scope doc |

---

## 6. Related docs

- Ground truth workflow: `Documentation/ai_driven_dev/linux_ground_truth.md`
- Stable checklist: `Documentation/STABLE.md` (update after green `smoke-release-0.0.1`)
- Roadmap backlog: `Documentation/ROADMAP.md`
